const ERROR = {
  dbConnection:
    "### database connection refused, check if the database is still running ### \n",
  apiError:
    "### api call refused, check if your are connected to the internet, and if you might have used all your free daily requests ### \n"
};

module.exports = {
  ERROR
};
